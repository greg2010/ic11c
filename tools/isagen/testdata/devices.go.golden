// Code generated by tools/isagen. DO NOT EDIT.

package isa

import "strconv"

// Access is the pair of directions the game allows on one property. AccessUnknown
// is decided from live state, so a caller must ask what a property refuses and
// never what it allows: against AccessReadWrite every unknown reads as a refusal.
type Access uint8

const (
	AccessNone Access = iota
	AccessRead
	AccessWrite
	AccessReadWrite
	AccessUnknown
)

var accessNames = [...]string{"none", "read", "write", "readwrite", "unknown"}

func (v Access) String() string {
	if int(v) >= len(accessNames) {
		return "Access(" + strconv.FormatUint(uint64(v), 10) + ")"
	}
	return accessNames[v]
}

// Prefab is one thing the game ships, and what a chip can reach on it once it is
// built. The surfaces never describe a device mid-build.
type Prefab struct {
	// Name is the prefab's own name, which the game hashes. Hash is the number a
	// batch instruction operand carries.
	Name string
	Hash int32
	// Title is the English name the game shows, empty for the few prefabs with none.
	Title string
	// CircuitHolder reports whether the thing can hold a chip, which is what makes
	// it reachable as db rather than only through a pin. CircuitHolderUnknown says
	// that could not be recovered, which is not the same as holding none.
	CircuitHolder        bool
	CircuitHolderUnknown bool
	// Slots is indexed by the slot number an ls or ss operand carries.
	Slots []Slot
	// Modes names the settings the Mode property selects between, indexed by the
	// number a program writes. It is empty when ModesUnknown is set.
	Modes        []string
	ModesUnknown bool
	// Logic lists the properties l, s and the batch forms reach, in LogicType
	// order. A property absent from it is one the device answers nothing for.
	Logic []LogicAccess
}

// Slot is one slot a prefab declares.
type Slot struct {
	// Class is the game's Slot.Class member naming what the slot accepts.
	Class string
	// Types lists the slot properties reachable here, in LogicSlotType order.
	Types []SlotAccess
}

// LogicAccess is one device property together with the directions the game
// allows on it. LogicType is the value the matching LogicTypes member carries.
type LogicAccess struct {
	LogicType uint16
	Allows    Access
}

// SlotAccess is [LogicAccess] for one slot's own properties. SlotType is the
// value the matching LogicSlotTypes member carries.
type SlotAccess struct {
	SlotType uint8
	Allows   Access
}

// Prefabs holds every thing the game ships, in the game's own roster order.
// Things with no logic surface are listed too, since Hash is what a batch
// operand carries: a hash naming none of these names nothing in this build.
// [LogicAccess] and [SlotAccess] are positional: property, then directions.
var Prefabs = []Prefab{
	{Name: "StructureSensor", Hash: 1812372242, Title: "Gas Sensor",
		Logic: []LogicAccess{{0, AccessRead}, {1, AccessRead}}},
	{Name: "StructureHousing", Hash: -1014431605, Title: "IC Housing", CircuitHolder: true,
		Modes: []string{"Number", "String"},
		Logic: []LogicAccess{{0, AccessRead}, {2, AccessReadWrite}},
		Slots: []Slot{
			{Class: "Helmet", Types: []SlotAccess{{0, AccessRead}, {1, AccessReadWrite}}},
			{Class: "Suit", Types: []SlotAccess{{0, AccessRead}, {1, AccessRead}}},
		}},
	{Name: "StructurePanel", Hash: 1110935274, ModesUnknown: true,
		Logic: []LogicAccess{{1, AccessReadWrite}, {2, AccessReadWrite}}},
	{Name: "StructureLouvre", Hash: 693815379,
		Modes: []string{"Retracted", "HalfOpen"},
		Logic: []LogicAccess{{0, AccessRead}, {1, AccessWrite}, {2, AccessReadWrite}}},
	{Name: "StructureMirror", Hash: -1960339480,
		Logic: []LogicAccess{{0, AccessUnknown}, {1, AccessUnknown}, {2, AccessUnknown}}},
	{Name: "ItemWrench", Hash: -1886261558},
}
