// Code generated by cmd/isagen. DO NOT EDIT.

package ic10

// ManifestID is the Steam depot manifest these tables were extracted from.
// It names the assembly's exact bytes, so a re-extraction that reaches it
// lands on this table again.
const ManifestID = "1000"

// GameVersion is the assembly file version these tables were extracted from.
const GameVersion = "0.0.1.2"

// Opcodes in game enum order. The numeric value is the ScriptCommand
// ordinal and is also the index into Instructions.
const (
	OpMove  Opcode = 0
	OpYield Opcode = 1
)

// Instructions holds every mnemonic the chip's assembler accepts, indexed by
// Opcode. Deprecated entries still assemble and run.
var Instructions = []Instruction{
	{Mnemonic: "move", Opcode: OpMove, Example: "move r? a(r?|num)", Operands: []Operand{{Kinds: []OperandKind{OperandRegister}, Direction: DirectionWrite}, {Name: "a", Kinds: []OperandKind{OperandRegister, OperandNumber}, Direction: DirectionRead}}},
	{Mnemonic: "yield", Opcode: OpYield, Example: "yield", Operands: nil},
}

// LogicTypes holds the device properties addressable by l, s, and the batch
// forms. Generated code emits Value rather than Name; they resolve alike.
var LogicTypes = []LogicTypeInfo{
	{Name: "None", Value: 0, Deprecated: true},
	{Name: "Power", Value: 1},
}

// LogicSlotTypes holds the slot properties addressable by ls, ss, and the
// batch slot forms.
var LogicSlotTypes = []LogicSlotTypeInfo{
	{Name: "None", Value: 0},
}

// BatchModes holds the aggregation modes accepted by the batch load forms.
var BatchModes = []BatchModeInfo{
	{Name: "Average", Value: 0},
}

// ReagentModes holds the reagent views accepted by lr.
var ReagentModes = []ReagentModeInfo{
	{Name: "Contents", Value: 0},
}

// Constants holds the numeric literals the chip's assembler accepts wherever a
// number is accepted, in game declaration order.
var Constants = []Constant{
	{Name: "pi", Value: 3.141592653589793},
	{Name: "rgas", Value: 8.31446261815324},
}
